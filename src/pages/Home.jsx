import React from 'react';
import usePageTitle from "../hooks/usePageTitle";

const Home = () => {
    usePageTitle('Accueil');
    return (
        <div>

        </div>
    );
};

export default Home;